This repository contains a machine learning project for ECG heartbeat classification using a 1D convolutional neural network. The workflow includes preprocessing, class imbalance handling with class weights, hyperparameter tuning, stratified cross-validation, final model evaluation, confusion matrices, ROC-AUC analysis, and discussion of precision-recall trade-offs in imbalanced medical data.
